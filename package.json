{
  "name": "bb-plugin-compact-panes",
  "version": "0.9.5",
  "type": "module",
  "description": "Tighter desktop panes, composer and sidebar, plus a configurable workspace accent.",
  "engines": {
    "bb": ">=0.42",
    "bbPluginSdk": ">=0.4.47"
  },
  "bb": {
    "name": "Compact UI",
    "description": "Tighter desktop panes, composer and sidebar, plus a themeable workspace accent.",
    "branding": {
      "icon": "GridView"
    },
    "server": "./server.ts",
    "app": "./app.tsx"
  },
  "scripts": {
    "build": "bb plugin build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "0.4.47",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
